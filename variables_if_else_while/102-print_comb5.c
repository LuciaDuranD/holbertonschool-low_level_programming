#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int a;
int b;
for (a = 0; a <= 99; a++)
{
for (b = a + 1; b <= 10; b++)
{
putchar(a / 10) + '0');
putchar(a % 10) + '0');
putchar(' ');
putchar(b / 10) + '0');
putchar(b % 10) + '0');
if (!(i == 98 && j == 99))
{
putchar(',');
putchar(' ');
}
}
}
putchar('\n');
return (0);
}
