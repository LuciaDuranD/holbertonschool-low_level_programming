#include "main.h"

/**
 * print_last_digit - main fuction
 *
 * @n: to print absolute value
 *
 * Return: last digit
 */
int print_last_digit(int);
{
int a;
a = n % 10;
if (x < 0)
a = a * (-1);
_putchar(a + '0');
return (a);
}
